---
phase: 01-foundation-public-pages
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/ui/video-hero.tsx
  - src/components/ui/button.tsx
  - src/components/sections/hero-section.tsx
  - src/components/sections/program-tiles.tsx
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/mobile-nav.tsx
  - src/app/(public)/layout.tsx
  - src/app/(public)/page.tsx
  - public/videos/.gitkeep
  - public/images/.gitkeep
autonomous: true

must_haves:
  truths:
    - "Visitor sees full-screen video hero on desktop (or poster image on mobile/slow connection)"
    - "Visitor can click CTA buttons on hero overlay"
    - "Visitor sees three program tiles below the hero"
    - "Visitor can navigate using desktop menu (logo left, nav center, CTA right)"
    - "Visitor can open mobile hamburger menu and navigate to all pages"
  artifacts:
    - path: "src/components/ui/video-hero.tsx"
      provides: "Video hero with poster fallback"
      min_lines: 40
      contains: "autoPlay"
    - path: "src/components/sections/program-tiles.tsx"
      provides: "Three program tiles grid"
      contains: "grid"
    - path: "src/components/layout/header.tsx"
      provides: "Navigation header"
      min_lines: 50
      contains: "navLinks"
    - path: "src/components/layout/mobile-nav.tsx"
      provides: "Mobile menu overlay"
      contains: "AnimatePresence"
    - path: "src/app/(public)/page.tsx"
      provides: "Homepage with hero and tiles"
      contains: "HeroSection"
  key_links:
    - from: "src/app/(public)/page.tsx"
      to: "src/components/sections/hero-section.tsx"
      via: "Component import"
      pattern: "import.*HeroSection"
    - from: "src/components/layout/header.tsx"
      to: "src/components/layout/mobile-nav.tsx"
      via: "Mobile menu toggle"
      pattern: "MobileNav|isOpen"
    - from: "src/components/ui/video-hero.tsx"
      to: "navigator.connection"
      via: "Connection detection"
      pattern: "navigator.*connection|effectiveType"
---

<objective>
Build the homepage with video hero, program tiles, and responsive navigation following the Anduril-inspired dark aesthetic.

Purpose: Create the primary visitor experience - the first impression that showcases RiseUp's programs with cinematic impact. The navigation enables access to all site pages.

Output: A complete homepage with autoplay video hero (mobile fallback), three program tiles linking to program pages, and responsive navigation with mobile hamburger menu.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-public-pages/01-RESEARCH.md
@.planning/phases/01-foundation-public-pages/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create video hero component with mobile fallback</name>
  <files>
    - src/components/ui/video-hero.tsx
    - src/components/ui/button.tsx
    - src/components/sections/hero-section.tsx
    - public/videos/.gitkeep
    - public/images/.gitkeep
  </files>
  <action>
    Create the VideoHero component following the pattern from 01-RESEARCH.md:

    src/components/ui/video-hero.tsx:
    - 'use client' directive (required for useState, useEffect)
    - Props: videoSrc, posterSrc, children (for overlay content)
    - State: canPlayVideo (boolean), isLoaded (boolean)
    - useEffect to detect mobile device and slow connection:
      - Check navigator.userAgent for mobile patterns
      - Check navigator.connection.effectiveType for 2g/slow-2g
      - Check navigator.connection.saveData for data saver mode
      - Only enable video on desktop with good connection
    - Render structure:
      - Full-screen container (h-screen w-full overflow-hidden)
      - Background poster image (always rendered, CSS background)
      - Video element (only rendered if canPlayVideo):
        - autoPlay muted loop playsInline preload="metadata"
        - Transition opacity from 0 to 100 when loaded
        - onLoadedData sets isLoaded to true
      - Gradient overlay (from-black/30 via-transparent to-black/60)
      - Children slot for content (centered, z-10)

    src/components/ui/button.tsx:
    - Reusable button component with variants
    - Props: variant ('primary' | 'secondary' | 'outline'), size ('sm' | 'md' | 'lg'), asChild, className
    - Primary: bg-accent text-black hover:opacity-90
    - Secondary: bg-white text-black hover:bg-white/90
    - Outline: border border-white text-white hover:bg-white/10
    - Use cn() utility for class merging
    - Support as Link via asChild pattern (or just use className on Link)

    src/components/sections/hero-section.tsx:
    - Import VideoHero and Button
    - Hardcoded content for now (will be CMS-driven in Phase 3):
      - Headline: "BUILDING CHAMPIONS ON AND OFF THE FIELD"
      - Subtext: "Youth football programs for ages 5-14 in [Location]"
      - Two CTAs: "Register Now" (primary, links to external registration), "Learn More" (outline, scrolls or links to programs)
    - Video/poster paths use placeholders (/videos/hero.mp4, /images/hero-poster.jpg)

    Create placeholder directories:
    - public/videos/.gitkeep
    - public/images/.gitkeep

    NOTE: Actual video/images will be added by the user. Use placeholder paths that work with next/image and standard video elements.
  </action>
  <verify>
    1. `npm run dev` - page loads without errors
    2. Resize browser to mobile width - video should NOT attempt to play (check Network tab)
    3. On desktop, video element should be in DOM (even without actual video file, element renders)
    4. Gradient overlay visible over poster/video area
    5. CTA buttons render with correct styling (lime accent for primary)
  </verify>
  <done>
    Video hero component displays with poster fallback, mobile detection working, CTA buttons styled correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Create responsive navigation with mobile menu</name>
  <files>
    - src/components/layout/header.tsx
    - src/components/layout/mobile-nav.tsx
    - src/components/layout/footer.tsx
    - src/app/(public)/layout.tsx
  </files>
  <action>
    Create navigation components following Anduril pattern (logo left, nav center, CTA right):

    src/components/layout/header.tsx:
    - 'use client' directive
    - Fixed position header (fixed top-0 z-50 w-full)
    - Background: transparent by default, can add bg-black/80 backdrop-blur on scroll (optional enhancement)
    - Navigation structure:
      - Logo (Link to /): "RiseUp" text or placeholder for logo image
      - Desktop nav (hidden md:flex): centered links
        - Flag Football (/flag-football)
        - Tackle Football (/tackle-football)
        - Academies & Clinics (/academies-clinics)
        - About (/about)
        - Contact (/contact)
        - Partners (/partners)
      - Desktop CTA (hidden md:block): "Donate" button linking to /ways-to-give
      - Mobile menu button (md:hidden): hamburger icon, toggles isOpen state
    - Pass isOpen state and setIsOpen to MobileNav component

    src/components/layout/mobile-nav.tsx:
    - 'use client' directive
    - Props: isOpen, onClose
    - Use Framer Motion AnimatePresence for enter/exit animations
    - When isOpen:
      - Full-screen black overlay (fixed inset-0 z-40 bg-black)
      - Centered vertical list of nav links (larger text, 3xl font)
      - Animate: fade in overlay, slide up links
    - Each link calls onClose when clicked
    - Include "Donate" link in mobile menu

    src/components/layout/footer.tsx:
    - Simple footer with:
      - RiseUp logo/name
      - Copyright: "(c) 2024 RiseUp Youth Football League"
      - Quick links: Programs, About, Contact, Partners
      - Social media placeholders (icons without actual links for now)
    - Dark background, muted text colors

    src/app/(public)/layout.tsx:
    - Create route group layout for public pages
    - Import and render Header at top
    - Main content area with {children}
    - Import and render Footer at bottom
    - Apply min-h-screen flex flex-col structure
  </action>
  <verify>
    1. Desktop (>768px): See logo left, nav links center, Donate button right
    2. Mobile (<768px): See logo left, hamburger button right
    3. Click hamburger: Full-screen menu animates in
    4. Click link in mobile menu: Menu closes, navigation works
    5. Footer visible at bottom of page
    6. All links point to correct paths (even if pages don't exist yet - 404 is expected)
  </verify>
  <done>
    Responsive navigation works on desktop and mobile, Framer Motion animations smooth, footer displays correctly
  </done>
</task>

<task type="auto">
  <name>Task 3: Create program tiles and assemble homepage</name>
  <files>
    - src/components/sections/program-tiles.tsx
    - src/components/ui/card.tsx
    - src/app/(public)/page.tsx
  </files>
  <action>
    Create program tiles section following Anduril's large tile grid pattern:

    src/components/ui/card.tsx:
    - Reusable card component for program tiles
    - Props: className, children
    - Base styles: rounded-lg overflow-hidden bg-white/5 border border-white/10
    - Hover state: hover:bg-white/10 transition-colors

    src/components/sections/program-tiles.tsx:
    - Three large tiles in a responsive grid:
      - Mobile: single column (grid-cols-1)
      - Tablet: 2 columns for first two, third full width (or 3 cols)
      - Desktop: 3 equal columns (lg:grid-cols-3)
    - Each tile is a Link to the program page:
      - Flag Football (/flag-football): "FLAG FOOTBALL" title, "Ages 5-8" subtitle, placeholder background image
      - Tackle Football (/tackle-football): "TACKLE FOOTBALL" title, "Ages 9-14" subtitle, placeholder background image
      - Academies & Clinics (/academies-clinics): "ACADEMIES & CLINICS" title, "All Ages" subtitle, placeholder background image
    - Tile structure:
      - Aspect ratio container (aspect-[4/3] or aspect-video)
      - Background image (CSS background or next/image with fill)
      - Gradient overlay for text legibility
      - Text at bottom: Program name (bold, xl-2xl), age range (sm, muted)
      - Hover effect: slight scale or brightness change

    src/app/(public)/page.tsx:
    - Import HeroSection and ProgramTiles
    - Page structure:
      - HeroSection (full viewport height)
      - ProgramTiles section (with padding, max-width container)
      - Optional: "Why RiseUp" teaser section (can be placeholder or omitted for Phase 1)
    - Metadata export: title, description for SEO

    Make sure the homepage uses the (public) route group layout by being placed in src/app/(public)/page.tsx
  </action>
  <verify>
    1. Homepage displays: video hero fills viewport, scroll down reveals program tiles
    2. Program tiles display in 3-column grid on desktop
    3. Program tiles stack on mobile (1 column)
    4. Clicking a tile navigates to correct path (/flag-football, etc.)
    5. Hover states work on tiles
    6. Page is scrollable, footer visible at bottom
  </verify>
  <done>
    Homepage complete with video hero, three program tiles in responsive grid, all navigation working
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Hero section**: Full-screen video area with poster fallback, CTAs visible
2. **Mobile detection**: On mobile viewport, video doesn't attempt to load (check Network tab)
3. **Navigation desktop**: Logo left, 6 nav links center, Donate button right
4. **Navigation mobile**: Hamburger menu opens full-screen overlay with animated links
5. **Program tiles**: 3 tiles visible below hero, responsive grid works
6. **Footer**: Displays at page bottom with copyright and links
7. **Performance**: Page loads quickly (video is deferred, not blocking)

Requirements covered:
- PAGE-01: Video hero with auto-play (muted, looping) - implemented
- PAGE-02: Overlay text with CTA buttons - implemented
- PAGE-03: Falls back to static image on mobile/slow connections - implemented
- PAGE-11: Navigation works with mobile hamburger menu - implemented
- TECH-01: Responsive (mobile-first, touch-friendly) - implemented
</verification>

<success_criteria>
- Video hero displays with autoplay on desktop, poster on mobile
- CTA buttons ("Register Now", "Learn More") are clickable and styled
- Three program tiles link to program pages
- Desktop navigation shows all links and Donate CTA
- Mobile hamburger menu animates smoothly and enables navigation
- Footer displays with copyright and quick links
- Page is fully responsive from mobile to desktop
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-public-pages/01-02-SUMMARY.md` using the summary template.
</output>
